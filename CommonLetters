  public int commonLetters(String guess, String orig)
	  { int ctr=0;
		  
		  for(int i=0;i<guess.length();i++)
		  { char x=guess.charAt(i);
		    if(orig.indexOf(x)>=1)
		    	   {  orig.get(guess.indexOf(x))=" ";
		    	ctr++;
		    	   }
			 
		  }
		  return ctr;
	  }
